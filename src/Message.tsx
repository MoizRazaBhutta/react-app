// Pascal casing first letter capital same as name of folder
function Message() {
  const name = "Moiz";
  // JSX is used here
  // IF ELSE in React
  if (name) return <h1>Hello {name}</h1>;
  return <h1>Hello World</h1>;
}

// We have to export it as default
export default Message;
